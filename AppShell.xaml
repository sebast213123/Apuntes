<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MosqueraAnthonny_TareaApuntes.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MosqueraAnthonny_TareaApuntes"
    Shell.FlyoutBehavior="Disabled"
    Title="MosqueraAnthonny_TareaApuntes">

    <Shell.FlyoutHeader>
        <Label Text="Mi Navegación"
               FontSize="24"
               HorizontalOptions="Center" />
    </Shell.FlyoutHeader>

    <TabBar>
        <ShellContent Title="Inicio" Icon="yo.jpg"
                      ContentTemplate="{DataTemplate local:InicioPage}" />
        <ShellContent Title="Acerca de" Icon="telequinesis.jpg"
                      ContentTemplate="{DataTemplate local:AboutPage}" />
        <ShellContent Title="Acerca de" Icon="perro.jpg"
                  ContentTemplate="{DataTemplate local:NotasPage}" />
    </TabBar>
</Shell>
